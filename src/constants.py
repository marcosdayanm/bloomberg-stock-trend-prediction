from pathlib import Path

PROJECT_ROOT = Path(__file__).parent.parent.resolve()

# Common routes
CRUDE_DATASETS_DIR = PROJECT_ROOT / "crude-datasets"
DATASETS_DIR = PROJECT_ROOT / "datasets"
SRC_DIR = PROJECT_ROOT / "src"